# antislop audit 001, fix report, 2026-09-15

Scope unchanged: `output/accommodation.html`, the three `output/room-*.html` pages, `css/pages/room-detail.css`, `css/pages/accommodation.css`, shared `css/components.css`, `js/contact.js`, `design.md`.
Rule from the owner: fix everything that can be fixed now; findings that depend on assets or pages the client has not delivered stay open.

## Fixed

| # | Rule | Change |
|---|---|---|
| 1 | R-02 | Em dashes replaced in the three chapter texts (accommodation), the Garden View Pool Villa description, the meta description, and all four `<title>` tags (now `Name | The Sankara Hill Penida: …`). |
| 5 | R-03 | `.button--pill` min-height raised from `--control-min-sm` (40px) to `--control-min` (44px). Header "Book Now" measures 121x44 on every page. `design.md` 3.4 now limits the 40px token to square icon controls. |
| 6 | R-25 | `.page-hero__scrim` gradient deepened at the bottom where the text sits (70% → 40% → 15%); breadcrumb, tagline and hero meta now render in full white instead of 70% / 85% alpha. Site-wide for every inner page hero. |
| 7 | R-10 | `backdrop-filter` removed from `.chapter__badge` and `.tag`; the 90-92% opaque fill carries them. Blur remains only on the scrolled header, slider arrow and lightbox backdrop. Documented in `design.md` 3.1. |
| 8 | R-26 | `contact.js` reads `?subject=` and `?room=` on load: preselects the topic and opens the message with "I am interested in the Ocean Hill Suite." (verified: `contact.html?subject=reservation&room=ocean-hill-suite#inquiry` lands with subject = reservation and the message prefilled). |
| 9 | R-08 | Arrows removed from the panel "Book Now" and "All Rooms"; no button on the room pages carries an arrow. Unused `i-arrow-*` symbols dropped from the sprites. |
| 10 | R-04 | Amenities are now a plain two-column list with the sage bullet, no icon per row; 17 amenity glyph symbols removed from each room sprite. `design.md` 3.7 records why Lucide (stroke weight matches Switzer 500 labels, quiet beside photography) and where icons are allowed. |
| 11 | C-3 | `.detail__strip` removed from the room pages (its two photos already sat in the gallery); the gallery tile that repeated the lead frame is gone. Each photo appears once below the hero. |
| 12 | R-05 | The standalone "Room Amenities" section is gone; the list lives inside the overview split under the lead photo (third child of `.detail`, `grid-row: 2` at ≥1024px, after the panel on phones so name, specs and Book Now stay next to the photo). Page rhythm is now hero → editorial split → gallery (surface) → info band → other rooms (bg) → CTA. Gallery grid on room pages is 3 columns with the 2x2 lead; `:nth-child(3n+1):last-child` / `(3n+2):last-child` rules close a short final row for any tile count WordPress renders. |
| 13 | C-1 | Listing chapters: one badge (room count only), subtitle is the type only ("Suite" / "Pool Villa"); size, view, bedding and pool live in the spec grid alone. Detail panel: the two tags over the lead frame are removed; kicker + spec grid remain. |
| 15 | R-15 | "Inquire" → "Ask about this suite" / "Ask about this villa". |
| 16 | Part 3 | `.spec__icon` colour moved from gold to `--color-soft` (site-wide, also home page spec rows). Gold on a chapter is now kicker, tagline, hairline and the gold button. `design.md` 3.1 updated. |
| 17 | Part 3 | `design.md` 1 now declares `Dials: ENERGY 1 / RHYTHM 2 / MOTION 2` with a one-line meaning. |
| 19 | R-19 | `data-reveal` removed from `.info__note` and `.cta__inner` on all four pages; reveals stay on section heads, chapters, grids and info items. |
| - | - | "Other Rooms / You May Also Like" → kicker "Compare", title "The Other Suites & Villas". |

## Still open (waiting on the client)

| # | Rule | Why it stays |
|---|---|---|
| 2 | R-24 | `blog.html` does not exist. Blog is planned; the mobile-menu and footer links stay until the page is built or the client drops the blog. |
| 3 | R-24 | `id/*.html` mirror is rendered by Polylang in WordPress, not in the static build. |
| 4 | R-38 | No per-room photo set yet. Galleries still reuse Lobby / Pool / hero / sibling-room photos; the "Photos coming soon" empty state exists and can be switched on per page when preferred. |
| 14 | R-16 | Room descriptions are the client's deck copy; rewriting with specifics (balcony depth, pool size) needs facts we do not have. Intro paragraph is still marked `TODO(content)`. |
| 18 | - | No square icon asset for a favicon; the wide logo SVG would render badly at 16px. Needs a monogram from the client. |
| 20 | R-17 | Footer "Hotel Information" figures come from the deck; `TODO(content)` stays until confirmed. |

## Evidence run (after fixes)

puppeteer-core + system Chrome, static server over `output/`:
- No horizontal overflow at 375 / 768 / 1024 / 1440 / 1920 on all four pages.
- Header "Book Now" 121x44 on all four pages.
- No `<use href>` pointing at a removed sprite symbol.
- No em dash in rendered body text.
- Accommodation tabs: Suites → 1 chapter, Pool Villas → 2, All → 3.
- Room pages: lead frame opens the lightbox, Escape closes it; gallery renders 4 tiles with no hole at 1440.
- Contact prefill: subject = reservation, message = "I am interested in the Ocean Hill Suite. ".
- No script errors in console.
- Screenshots reviewed at 1440 and 375 for overview, gallery, other rooms, listing hero.

## Delivery Gate, these pages

Block 1: R-02 PASS, R-03 PASS (overflow 0, 44px controls), R-17 PASS (deck figures, TODO noted), R-18 PASS, R-23 OPEN (photos, finding 4), R-24 OPEN (findings 2, 3), R-25 PASS (white on ≥40% black band), R-26 PASS, R-27 PASS, R-28 n/a, R-32 PASS, R-33 PASS, R-34 n/a, R-35 PASS (run above), R-36 PASS, R-37 PASS (design.md with dials), R-38 OPEN (finding 4).
Block 2: all PASS after 7, 9, 10, 19.
Block 3: dials declared, one focal point per screen (hero title; lead photo + panel; gallery lead), one accent (gold), motif (serif title + gold hairline + editorial split). PASS.
Block 4: C-3 PASS after 11, 12; R-15 PASS after 15; R-16 OPEN (finding 14).

Not shippable until findings 2, 3, 4 are resolved by the client; everything else is clean.
