# Audit antislop 003, laporan perbaikan, 2026-09-15

Cakupan sama: `output/spa.html`, `output/dining.html`, `css/pages/spa.css`, `css/pages/dining.css`, komponen bersama di `css/components.css`, `js/main.js`, `components.html`, `design.md`. Dua perubahan menyentuh halaman lain: `home.html` (pil ikon fasilitas dibuang, pola yang sama dengan spa) dan tinggi sentuh tautan footer serta breadcrumb (seluruh situs lewat `components.css`).
Aturan dari pemilik: perbaiki semua yang bisa diperbaiki sekarang; yang bergantung pada aset atau konten klien tetap terbuka atau memakai konten dummy yang diberi label; perubahan desain diizinkan bila hasilnya kurang generik.

## Sudah diperbaiki

| # | Aturan | Perubahan |
|---|---|---|
| 41 | R-38 / R-23 | Hero spa diganti `Villa.jpeg` (teras resor saat senja, alt jujur). `spa.jpg` tidak lagi dipakai di spa.html: kartu paket tidak berfoto (lihat 45), tile galeri dihapus (lihat 42). Komentar `assumption` mencatat foto pengganti. |
| 42 | R-38 | Semua alt dan caption yang mengarang adegan dihapus atau dijujurkan. Chapter spa: "Suite interior ... with woven textures". Fasilitas: `hero_resort` jadi "The resort on its ridge", `Lobby.jpeg` jadi "Open-air lobby pavilion". Chapter dining: "Open-air lobby pavilion ... looking out over the hills". Galeri spa dan dining dikosongkan: `.gallery__grid` `hidden`, empty state "Spa photos coming soon" / "Dining photos coming soon" tampil, tombol "View Full Gallery" tetap. Komentar `TODO(content)` menjelaskan WordPress mengisi grid dan menyembunyikan empty state saat media tag spa/dining dipublikasikan. |
| 43 | R-38 / R-17 | Dining: jam sarapan 07.00–10.30, "Last order 21.30", "Included for in-house guests", "Sunset is around 18.15", "groups of six or more", "the day's catch from the island's fishermen", "private dinner set-up on request" semuanya dihapus. Tabel jam kini tiga baris persis dari CW (Puṇṇa 07.00–22.00, in-villa 07.00–22.00, Sukha 10.00–22.00). Pengantar jam jadi "Every venue is open daily. Message us on WhatsApp to reserve a table or a private dinner." Komentar `TODO(content)` menandai baris yang bisa ditambah setelah klien konfirmasi. |
| 44 | R-38 | Spa: pengantar treatments jadi kalimat CW ("Holistic treatments inspired by Balinese healing traditions, guided by experienced therapists."). CTA jadi "Reserve a treatment before you arrive, or ask the spa for today's availability." Paket tidak lagi menjanjikan makan siang di Puṇṇa, koktail di Sukha, atau yoga matahari terbit. |
| 45 | R-38 | Label placeholder yang terlihat: `.menu-list__note` (hairline emas kiri, italic, `--color-soft`) di atas daftar treatment ("Sample list. The confirmed treatment menu and prices will replace it."), paket ("Sample journeys. Inclusions and prices will be confirmed by the spa."), dan menu dining ("Sample menu. The confirmed menu and prices will replace it."). Komponen didokumentasikan di `components.html` dan `design.md`. Konten contoh tetap ada sesuai izin pemilik. |
| 46 | R-26 / R-25 | Tombol "Download Spa Menu (PDF)" dan "Download Menu (PDF)" dihapus beserta `aria-disabled`, simbol `i-download`, dan rule opasitas 0.55 di kedua CSS halaman. Section head kembali ke bentuk stack. Komentar `TODO(content)` menyatakan WordPress merender tombolnya hanya setelah URL PDF ada. `design.md` 3.4: tautan tidak pernah dirender dengan `aria-disabled`. |
| 47 | R-03 | `.breadcrumb__item a::after` memperluas area sentuh ke 44px tanpa mengubah tampilan 19px (seluruh halaman dalam). `.footer__list a` jadi `inline-flex` dengan `min-height: 44px`; diukur 61x44 di 375 (seluruh situs). `design.md` 3.4 diperbarui. |
| 48 | R-08 | Panah dibuang dari "View Treatments" dan "See the Menu"; simbol `i-arrow-right` dihapus dari sprite spa (dining masih memakainya untuk "The Infinity Pool →" ke about.html). Aturan di `design.md` 3.4 ditambah: tombol anchor dalam halaman tanpa panah. |
| 49 | R-04 | `i-sparkles` diganti `i-flower-2` di empty state paket; simbol sparkles dihapus dari sprite spa dan home. |
| 50 | R-04 | (a) Ikon per baris dibuang dari tabel jam dining (simbol coffee, wine, bed-double dihapus). (b) `.amenity__icon` dibuang dari ketiga kartu fasilitas spa dan keempat kartu di `home.html` (simbol sun, dumbbell, waves, mountain-snow, flower-2, utensils-crossed, sparkles dihapus dari sprite yang tidak lagi memakainya). `index.html` (phase 1, dibekukan) tidak disentuh, jadi rule CSS-nya tetap. (c) `.card__meta svg` kini `--color-soft`, sesuai `design.md` 3.1. `design.md` 3.7 diperbarui. Tabel jam di about, experiences, faq masih berikon (di luar cakupan). |
| 51 | R-09 | Badge "Balinese healing traditions" dihapus; chapter spa menyisakan badge jam. Simbol `i-leaf` dihapus. Badge kedua di chapter dining ("In-villa dining 07.00 to 22.00") juga dihapus karena jamnya ada di tabel. |
| 52 | R-05 / C-1 | "A Day at Puṇṇa" dibangun ulang: tiga entri teks (Breakfast, Lunch and Dinner, In-villa Dining) di atas satu hairline, tiga kolom di ≥768px, tanpa nomor, tanpa jam, tanpa foto. Teksnya diturunkan dari CW (indoor/alfresco, Indonesian dan Mediterranean dari bahan lokal, in-villa dining ke balkon suite atau tepi kolam villa). Jam Puṇṇa kini tampil dua kali di halaman (badge chapter dan tabel), bukan empat. `.steps` dan `.dining-day__figure` dihapus dari `dining.css`. |
| 53 | C-3 | Spa: setiap foto sekali di bawah hero (`sankara-deluxe-hill` chapter; `hero_resort`, `Lobby`, `Pool` fasilitas). Dining: `Lobby` chapter, `Pool` Sukha. Galeri kosong sampai foto venue tiba (lihat 42). |
| 54 | R-26 / C-2 | `main.js` `updateArrows` menyembunyikan kedua panah (`hidden`) bila `scrollWidth - clientWidth <= 1`, dicek juga saat resize. Diuji: spa 768 / 1024 / 1440 panah hidden; home 1440 (empat kartu, overflow 389px) panah tetap tampil. `design.md` tabel Amenity slider diperbarui. |
| 55 | Ritme | Selesai lewat 52: seksi hari tidak lagi split media-kiri; urutan dining kini chapter (foto kiri) → tiga kolom teks → menu → chapter flip (foto kanan) → galeri → jam + band. |
| 56 | R-15 | Ghost "Book Now" dihapus dari band CTA spa dan dining; tinggal satu tombol emas per band. Header dan menu mobile tetap membawa Book Now. `design.md` baris CTA band diperbarui. |
| 57 | Konsistensi | Kicker ditambahkan: spa "Journeys", "Wellness", "Gallery"; dining "All-day dining", "Gallery", "Hours". Kedua halaman kini kicker di setiap section head; chapter memakai subtitle. `design.md` 10 dicatat. |
| 58 | R-03 | `.hours__row dt { flex: 1 1 0; min-width: 0 }` sehingga label membungkus dan nilai tetap rata kanan. Diuji di 375: ketiga baris dining sejajar; enam baris contact dan empat belas baris about `dd` tetap rata kanan di x=355. |
| 59 | R-19 | `data-reveal` dibuang dari `.cta__inner` di kedua halaman; langkah dan kartu paket tidak lagi jadi target masing-masing. Sisa 11 reveal per halaman (section head, chapter media/body, daftar, grid, kartu fasilitas). |
| 60 | C-1 | Tagline hero spa jadi "Balinese healing traditions, practised high above the island."; jam buka tinggal di badge chapter (dan meta description). |
| - | - | Checklist QA di kaki kedua file ditulis ulang mengikuti struktur baru. |

## Masih terbuka (menunggu klien)

| # | Aturan | Alasan |
|---|---|---|
| 41 (sisa) | R-38 | `spa.jpg` (kamar mandi) masih dipakai sebagai foto spa di `home.html` (3x), `index.html` (2x), `about.html`, `gallery.html`; di luar cakupan audit ini. Perlu foto spa dari klien, lalu ganti di empat halaman itu. |
| 42 / 53 (sisa) | R-38 | Galeri spa dan dining kosong sampai set foto venue (Google Drive di CW) diberikan. Foto fasilitas (yoga, gym) masih memakai foto resor dan lobi dengan alt jujur dan komentar `assumption`. |
| 43 (sisa) | R-17 | Jam sarapan, last order, dan kebijakan reservasi menunggu konfirmasi klien sebelum ditambah ke tabel. |
| 45 (sisa) | R-38 | Daftar treatment, paket, dan menu masih contoh berlabel; diganti saat menu klien tiba. Tombol PDF dirender WordPress setelah URL ada (46). |
| 50 (sisa) | R-04 | Ikon per baris di tabel jam `about.html`, `experiences.html`, `faq.html` belum disentuh (halaman belum diaudit). |
| 001-#2 | R-24 | `blog.html` belum ada. |
| 001-#3 | R-24 | Mirror `id/*.html` dirender Polylang di WordPress. |
| 001-#18 | R-35 | Favicon: belum ada aset monogram persegi. |
| 001-#20 | R-17 | Angka "Hotel Information" di footer masih dari deck. |

## Bukti pengujian (setelah perbaikan)

puppeteer-core + Chrome sistem, server statis di atas `output/`:
- Tidak ada overflow horizontal di 375 / 768 / 1024 / 1440 / 1920 / 2560 pada spa, dining; contact, about, home dicek ulang di 375.
- Console: spa hanya 404 favicon (terbuka); dining bersih. Tidak ada `<use href>` ke simbol yang hilang di spa, dining, home.
- Tidak ada `a[aria-disabled]` di kedua halaman; tombol PDF tidak ada.
- Galeri: grid `hidden`, empty state terlihat, 0 `[data-lightbox-item]`; "View Full Gallery" → `gallery.html#spa` / `#dining` (chip dipilih oleh `gallery.js`).
- Label sampel: 2 di spa, 1 di dining. Kicker: spa Treatments / Journeys / Wellness / Gallery / Reservations; dining All-day dining / The Menu / Gallery / Hours / Reservations.
- Band CTA: 0 tombol ghost, tidak ada `data-reveal`. Reveal per halaman: 11.
- Ikon: 0 `.amenity__icon`, 0 svg di `.hours__row`, 0 `.steps`. Panah outline: spa tidak ada; dining hanya `i-arrow-right` di "The Infinity Pool" (ke about.html).
- Slider spa: panah `hidden` di 768 / 1024 / 1440; home 1440 panah tampil (overflow 389px).
- Menu mobile: toggle membuka, Escape menutup. Tombol anchor "View Treatments" / "See the Menu": hash berubah, target 48px di bawah tepi atas.
- Target sentuh di 375: breadcrumb 38x19 dengan `::after` 44px; tautan footer 61x44; sisa di bawah 44px hanya skip-link (fokus keyboard) dan dua tautan kontak footer 24px tinggi (teks inline, di luar cakupan).
- Tabel jam: tiga baris dining sejajar di 375; contact dan about `dd` rata kanan di x=355.
- Screenshot ditinjau di 1440 dan 375: hero spa, treatments, journeys, galeri kosong, band CTA; dining day, hours + band, footer.

## Delivery Gate, kedua halaman (setelah perbaikan)

Blok 1 Hard Gate: R-24 masih FAIL (001 #2, #3), R-35 masih FAIL (favicon 404). R-38 PASS untuk konten yang tampil (semua contoh berlabel, foto dengan alt jujur, klaim tanpa sumber dihapus); sisa R-38 ada di halaman lain (`spa.jpg`). R-23, R-25, R-26, R-03, R-02, R-27, R-32 PASS.
Blok 2 Purpose-Gate: PASS (48, 49, 50, 51, 53, 54 selesai; alasan ditulis di `design.md`).
Blok 3 Liveliness: PASS. Dial ENERGY 1 / RHYTHM 2 / MOTION 2 terjaga; pemecah ritme per halaman: spa = slider fasilitas bleed, dining = tiga kolom teks di atas hairline + chapter flip.
Blok 4 Quality Lock: PASS untuk 52, 56, 57, 59, 60. R-17 tetap menunggu 001-#20.

Halaman belum bisa dinyatakan lolos gate selama blog, mirror ID, dan favicon belum diberikan klien; konten contoh (menu, treatment, paket) dan galeri kosong menunggu materi klien.
